The page sorexss.php reads and displays content from a file without sanitizing it. The content is user-supplied which means that it will allow any JavaScript inside storedxss.txt to be executed in the browser.